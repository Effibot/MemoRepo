<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.Material" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:textColorHint">@color/titleColor</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">@color/titleColor</item>
    </style>

    <style name="cardStyle" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/cardBgStdColor</item>
        <item name="cardCornerRadius">10dp</item>
        <item name="cardElevation">6dp</item>
    </style>
    <style name="materialTextInput" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="android:layout_weight">3</item>
        <item name="android:paddingStart">15dp</item>
        <item name="android:paddingEnd">27dp</item>
        <item name="android:layout_marginStart">12dp</item>
        <item name="android:inputType">textMultiLine</item>
        <item name="android:maxLines">2</item>
        <item name="android:maxRows">2</item>
        <item name="boxStrokeColor">@color/hintColor</item>
        <item name="android:textColorHint">@color/titleColor</item>
        <item name="hintTextColor">@color/hintColor</item>
        <item name="android:backgroundTint">@android:color/transparent</item>
        <item name="android:textSize">25sp</item>
        <item name="android:importantForAutofill">no</item>
    </style>

    <style name="materialButton" parent="Widget.MaterialComponents.ExtendedFloatingActionButton.Icon">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="cornerRadius">4dp</item>
        <item name="backgroundTint">@color/colorPrimaryDark</item>
        <item name="elevation">6dp</item>
        <item name="android:textAlignment">textEnd</item>
        <item name="android:textAppearance">@style/ButtonTextAppearance</item>
        <item name="android:textColor">@color/titleColor</item>
        <item name="android:textSize">10sp</item>
        <item name="iconPadding">0dp</item>
        <item name="iconTint">#FFFFFFFF</item>
        <item name="android:strokeColor">@color/titleColor</item>
        <item name="android:layout_marginBottom">4dp</item>
    </style>

    <style name="ButtonTextAppearance" parent="TextAppearance.MaterialComponents.Button">
        <item name="android:textAllCaps">true</item>
    </style>
</resources>
